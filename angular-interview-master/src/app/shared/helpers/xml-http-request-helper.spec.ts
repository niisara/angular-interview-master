import { XmlHttpRequestHelper } from './xml-http-request-helper';

describe('XmlHttpRequestHelper', () => {
  it('should create an instance', () => {
    expect(new XmlHttpRequestHelper()).toBeTruthy();
  });
});
